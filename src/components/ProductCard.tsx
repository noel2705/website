import React from 'react';
import AddToCart from "@/components/AddToCart";

const ProductCard = () => {
    return (
        <div>
            <AddToCart/>
        </div>
    );
}

export default ProductCard;
